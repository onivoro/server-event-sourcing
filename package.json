{
  "name": "@onivoro/server-event-sourcing",
  "version": "0.0.2",
  "dependencies": {
    "@nestjs/common": "^7.6.12",
    "rxjs": "^6.6.3"
  },
  "scripts": {
    "test": "jest",
    "build": "tsc -p tsconfig.json",
    "release": "rm -rf dist && npm run build && npm version minor && cp package.json dist && cd dist && npm publish --access public"
  },
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "@types/node": "^14.11.2",
    "jest": "^26.4.2",
    "ts-jest": "^26.4.1",
    "typescript": "^4.0.3"
  }
}
